The package.json isn't a 'real one' - we are just experimenting with using something like that format for our plugins.
The 'scripted' section within it isn't used yet but could include metadata to make locating and working with plugins easier.
